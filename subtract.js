//We use a generic function name here so that we can use the same code to import and eval many functions
exports.subtractNumber = function (num, val) {
    return num - val;
  }
  module.exports = exports;
